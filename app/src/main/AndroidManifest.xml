<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!--
      android.permission.MANAGE_MEDIA is deliberately NOT declared.
      Its protection level is signature|appop|preinstalled, so a normally
      installed app can never hold it: MediaStore.canManageMedia() returns false
      with or without the declaration. Nothing in the app branches on it - it is
      only written to a diagnostic log - and the direct IS_TRASHED write already
      falls back to the system confirmation dialog
      (MediaStore.createTrashRequest) when it raises SecurityException, which is
      the supported flow. Declaring it only widened the capability surface
      without granting any access.
    -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:name=".MyDriveApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Company-owned device activity monitoring. The service is only bound by
          Android after an authorised user enables it in Accessibility settings,
          and it must be declared with BIND_ACCESSIBILITY_SERVICE.
        -->
        <service
            android:name=".data.accessibility.MyDriveAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_monitoring_service_summary"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_monitoring_config" />
        </service>

    </application>
</manifest>
